# bash-shell-script-playground

learn and experiment with [bash](https://www.gnu.org/software/bash/) shell scripts

## Development Workflow

[`main.sh`](main.sh)

```sh
# re-run on changes to main.sh.  run in own shell / blocks
make dev

# edit main.sh
```